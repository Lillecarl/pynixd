target-version = "py313"
line-length = 120

# The gate. `checks.lint` and `checks.format` in default.nix read this file,
# and ruff finds it by proximity, so do not add a `[tool.ruff]` to
# `pyproject.toml` beside it: a `ruff.toml` in a directory wins over the
# `pyproject.toml` in the same directory, and one used to sit there looking
# like the configuration of this project while governing nothing.
#
# `nix-daemon-protocol/` carries its own `pyproject.toml`, so ruff reads that
# for those files. Passing `--config ruff.toml` overrides the proximity rule
# and reports import-sort findings there that the gate does not; run ruff
# without `--config` to see what CI sees.
#
# `ruff-strict.toml` beside this file is the target, not a gate: 66 findings
# on 2026-09-16. It extends this file, so a change here reaches it.

[lint]
select = [
    "E", "W",  # pycodestyle
    "F",       # pyflakes (real bugs)
    "I",       # isort
    "TID",     # flake8-tidy-imports, for the banned-api table below
    "UP",      # pyupgrade, which used to be a separate `just upgrade` run
]

ignore = [
    "E501",  # line length handled by line-length setting
    "TID252",  # relative imports, which this project uses by design
]

# `I` is selected, and without this section isort sorts this tree's own
# absolute imports as third-party packages. Each name needs saying:
#
# - `nix_daemon_protocol` lives at `nix-daemon-protocol/src/nix_daemon_protocol`,
#   so no directory of that name sits beside this file for ruff to find.
# - `tests` is this project's suite. It has no `src/` layout either, and its
#   modules import each other as `tests.something`.
# - `pynixd` is beside this file, and ruff still sorted `pynixd.serde.ids`
#   into the third-party block while it put `pynixd.serde` in the first-party
#   one. One project cannot be in two groups.
[lint.isort]
combine-as-imports = true
known-first-party = [
    "nix_daemon_protocol",
    "pynixd",
    "tests",
]

# The repository writes concurrency in one vocabulary, and that vocabulary is
# anyio. This project starts with `anyio.run(backend="asyncio", ...)` in
# `__main__.py`, so an anyio primitive works anywhere in it. Issue #131 ported
# the remaining 48 uses.
#
# This table lives here only. `ruff-strict.toml` has no banned-api table, so
# there is nothing to keep it equal to.
#
[lint.flake8-tidy-imports.banned-api]
"asyncio.get_event_loop".msg = "Use asyncio.get_running_loop() inside async code; time.monotonic() for timestamps."
"asyncio.set_event_loop".msg = "anyio.run() owns the event loop; do not install a process-global default underneath it."
"asyncio.new_event_loop".msg = "Use anyio.run(), which creates and owns the loop."
"asyncio.get_event_loop_policy".msg = "Select a backend via anyio.run(backend=...) rather than a global loop policy."
"asyncio.set_event_loop_policy".msg = "Select a backend via anyio.run(backend=..., backend_options=...) rather than a global loop policy."
"asyncio.to_thread".msg = "Use anyio.to_thread.run_sync(fn, *args) -- bounded by a CapacityLimiter, unlike asyncio's unbounded spawn. It does NOT forward **kwargs: wrap keyword args in functools.partial."
"asyncio.wrap_future".msg = "Prefer anyio.to_thread / anyio.from_thread.BlockingPortal."
"asyncio.sleep".msg = "Use anyio.sleep()."
"asyncio.Lock".msg = "Use anyio.Lock()."
"asyncio.Event".msg = "Use anyio.Event(). Note it has no clear(): PressureFlag in monitor.py shows how to re-arm."
"asyncio.Semaphore".msg = "Use anyio.Semaphore(initial_value)."
"asyncio.BoundedSemaphore".msg = "Use anyio.Semaphore(n, max_value=n)."
"asyncio.Condition".msg = "Use anyio.Condition()."
"asyncio.TaskGroup".msg = "Use `async with anyio.create_task_group() as tg:`, and `tg.start_soon(fn, *args)`."
"asyncio.timeout".msg = "Use `with anyio.fail_after(seconds):` (or move_on_after to expire silently)."
"asyncio.timeout_at".msg = "Use `with anyio.CancelScope(deadline=...):`, against anyio.current_time()."
# ═══════════════════════════════════════════════════════════════
# END SHARED
# ═══════════════════════════════════════════════════════════════

# WireModel.from_reader / _wire_fields / _find_reader all resolve type
# annotations at runtime (get_type_hints + issubclass checks). Without this,
# TC rules flag imports used only as WireModel field annotations, forcing
# pervasive # noqa: TC001 noise.
#
# `ruff-strict.toml` inherits this through `extend`, so it is written once.
[lint.flake8-type-checking]
runtime-evaluated-base-classes = ["nix_daemon_protocol.wire_message.WireModel"]
